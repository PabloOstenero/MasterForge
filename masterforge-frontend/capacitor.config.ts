import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.masterforge.app',
  appName: 'masterforge-frontend',
  webDir: 'www'
};

export default config;
